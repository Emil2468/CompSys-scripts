{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run VMCache",
            "type": "shell",
            "command": "cd ${workspaceRoot} && node VMCache.js",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}